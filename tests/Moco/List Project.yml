info:
  name: List Project
  type: http
  seq: 6

http:
  method: GET
  url: "{{HOST}}/api/v1/projects/947219244"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
