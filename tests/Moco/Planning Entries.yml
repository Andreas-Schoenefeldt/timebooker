info:
  name: Planning Entries
  type: http
  seq: 14

http:
  method: GET
  url: "{{HOST}}/api/v1/planning_entries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
