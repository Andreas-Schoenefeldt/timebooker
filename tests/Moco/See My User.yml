info:
  name: See My User
  type: http
  seq: 11

http:
  method: GET
  url: "{{HOST}}/api/v1/users/{{USER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
