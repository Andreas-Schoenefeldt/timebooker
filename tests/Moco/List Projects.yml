info:
  name: List Projects
  type: http
  seq: 5

http:
  method: GET
  url: "{{HOST}}/api/v1/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
