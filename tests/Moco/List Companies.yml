info:
  name: List Companies
  type: http
  seq: 13

http:
  method: GET
  url: "{{HOST}}/api/v1/companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
