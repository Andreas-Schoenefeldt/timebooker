info:
  name: List User Permissions
  type: http
  seq: 10

http:
  method: GET
  url: "{{HOST}}/api/v1/users/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
