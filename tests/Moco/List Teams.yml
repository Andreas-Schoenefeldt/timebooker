info:
  name: List Teams
  type: http
  seq: 12

http:
  method: GET
  url: "{{HOST}}/api/v1/units"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
