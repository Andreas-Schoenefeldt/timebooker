info:
  name: List Project Tasks
  type: http
  seq: 8

http:
  method: GET
  url: "{{HOST}}/api/v1/projects/947219244/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
