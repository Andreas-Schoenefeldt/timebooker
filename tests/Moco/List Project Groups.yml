info:
  name: List Project Groups
  type: http
  seq: 7

http:
  method: GET
  url: "{{HOST}}/api/v1/projects/groups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
